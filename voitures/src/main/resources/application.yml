
server:
  port: 8082

spring:
  application:
    name: SERVICE-CAR
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:cardb
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
